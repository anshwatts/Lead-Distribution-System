/** @type {import('next').NextConfig} */
const nextConfig = {
  // No strict mode if you want to avoid double mounting in dev (optional, but good for testing locks sometimes)
  // reactStrictMode: true,
};

module.exports = nextConfig;
